# Build artifacts
/bin/
*.test
*.out

# Accidental cmd binaries built from daemon/ without -o (source lives in daemon/cmd/<name>/)
/daemon/seqob*
/daemon/compaged
/daemon/oceand

# Go workspace (kept local; not committed until the daemon/wallet modules land)
go.work
go.work.sum

# Editor / OS
.DS_Store
.idea/
.vscode/

# Local env / secrets
*.env
!*.example.env
datadir/

# local build outputs (scp'd to the box)
/dist/

# Python bytecode from the regtest proofs
__pycache__/
*.py[cod]

# Agent worktrees and local agent state (never belongs in the repo).
.claude/
